#!/usr/bin/env bash
set -euo pipefail

# Auto-format staged Rust files and re-stage them so commits are always clean.
staged_rs=$(git diff --cached --name-only --diff-filter=ACM -- '*.rs')
[ -z "$staged_rs" ] && exit 0

cargo fmt --all
echo "$staged_rs" | xargs git add
