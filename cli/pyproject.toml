[project]
name = "expert-cli"
version = "0.1.0"
description = "CLI for the Expert attention fabric system"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12",
    "httpx>=0.27",
    "rich>=13",
]

[project.scripts]
expert = "expert_cli.main:app"
